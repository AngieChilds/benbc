<div class="row">    
    
    <div class="large-9 push-3 columns" >


<%= form_for(@bookmark) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit "New weblink"%>
  </div>
<% end %>
      <%= link_to 'Back', bookmarks_path %>
    </div>
</div>

      